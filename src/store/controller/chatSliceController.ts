import { MAP_SIZE } from 'util/const';
import { customLog } from 'util/util';
import type {} from '../../type/gameType';
// 채팅 관리자
export const chatUtil = {
  // 공지하다
  temp1: function (str: string) {
    return;
  },
  // 보내다
  // 받다
};
